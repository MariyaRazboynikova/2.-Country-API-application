import 'package:flutter/material.dart';

class CountryDetailes extends StatelessWidget {
  const CountryDetailes({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
